version: '2'
services:
  toga:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8080:8080
    links:
      - redis
    command: npm start -- --config './app/config/application.json' --config './app/config/productionOverrides.json'
  redis:
    image: redis
