version: '2'
services:
  toga:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8080:8080
    links:
      - redis
    command: sh ./script/docker_compose_start
  redis:
    image: redis
